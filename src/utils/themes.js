import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    primary: "Montserrat",
  },
  colors: {
    primary: "#093450",
    secondary: "#F3F5F8",
    blaq: "#474747",
  },
});

export default theme;
